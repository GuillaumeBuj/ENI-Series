{% extends 'base.html.twig' %}

{% block title %}
    Create | {{ parent() }}
{% endblock %}

{% block body %}
    {{ form_start(serieForm) }}

    {{ form_widget(serieForm) }}
    <button>Create</button>

    {{ form_end(serieForm) }}
{% endblock %}

